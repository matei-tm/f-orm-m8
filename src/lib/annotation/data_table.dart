/// DataTable describe the
/// generated table
class DataTable {
  /// The name of the table
  final String name;

  /// A bit mask formed by a combination of TableMetadata
  final int metadataLevel;

  /// Constructor to define the annotation parameters
  const DataTable([this.name, this.metadataLevel]);
}
